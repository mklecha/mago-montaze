import {StyleSheet} from "react-native";

export const headerStyles = StyleSheet.create({
    title: {
        color: '#fff',
        fontSize: 25,
        fontWeight: "bold"
    },
    settingsIcon: {
        justifyContent: "center"
    }
});
